import {useState} from 'react';import {orderService} from '../../services/orderService';import {formatCurrency} from '../../utils/format';import {orderStatusClass,orderStatusLabel} from '../../utils/orderStatus';
export function OrdersPage(){const[,refresh]=useState(0);const orders=orderService.getAll();function cancel(id:string){const reason=prompt('Motivo do cancelamento:');if(reason){orderService.cancel(id,reason);refresh(x=>x+1)}}return <div><h1 className="text-3xl font-black">Histórico de pedidos</h1><div className="mt-6 overflow-x-auto rounded-2xl bg-white"><table className="w-full min-w-[950px] text-left text-sm"><thead><tr className="border-b bg-slate-50"><th className="p-4">Horário</th><th>Cliente</th><th>Pedido</th><th>Valor</th><th>Situação</th><th>Entregador</th><th>Ações</th></tr></thead><tbody>{orders.map(o=><tr key={o.id} className="border-b"><td className="p-4">{new Date(o.createdAt).toLocaleString('pt-BR')}</td><td><b>{o.customer.name}</b><br/><small>{o.customer.phone}</small></td><td>{o.items.map(i=>`${i.quantity}x ${i.product.name}`).join(', ')}</td><td>{formatCurrency(o.total)}</td><td><span className={`rounded-full px-3 py-1 text-xs font-bold ${orderStatusClass[o.status]}`}>{orderStatusLabel[o.status]}</span>{o.cancellationReason&&<div className="mt-1 text-xs text-slate-500">{o.cancellationReason}</div>}</td><td>{o.assignedCourierName||'—'}</td><td>{!['cancelled','delivered'].includes(o.status)&&<button onClick={()=>cancel(o.id)} className="text-red-600">Cancelar</button>}</td></tr>)}{!orders.length&&<tr><td colSpan={7} className="p-12 text-center text-slate-500">Nenhum pedido registrado ainda.</td></tr>}</tbody></table></div></div>}
