import {useState} from 'react';import {useNavigate} from 'react-router-dom';import {Button} from '../../components/common/Button';import {Input} from '../../components/common/Input';import {authService} from '../../services/authService';import {storageService} from '../../services/storageService';import {useAuth} from '../../contexts/AuthContext';export function VerificationPage(){const[code,setCode]=useState('');const nav=useNavigate();const{setCustomer}=useAuth();const pending=storageService.get<{name:string;phone:string}>('pending-customer',{name:'Cliente',phone:''});function confirmCode(){if(!authService.verifyCode(code))return alert('Digite um código com 6 números.');const c=authService.saveCustomer(pending.name,pending.phone);setCustomer(c);alert('Código confirmado com sucesso.');nav('/checkout/endereco')}return <div className="mx-auto max-w-md px-4 py-16 text-center"><h1 className="text-3xl font-black">Verifique seu telefone</h1><p className="mt-3 text-slate-500">Enviamos um código para {pending.phone}. Nesta demonstração, qualquer código com 6 números é válido.</p><Input className="mt-8 text-center text-2xl tracking-[0.4em]" maxLength={6} value={code} onChange={e=>setCode(e.target.value.replace(/\D/g,''))}/><Button className="mt-4 w-full" onClick={confirmCode}>Confirmar código</Button><button className="mt-4 text-sm text-brand-600">Reenviar código</button></div>};