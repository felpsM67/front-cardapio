import {createContext,useContext,useState,type ReactNode} from 'react';import type {Customer} from '../models';import {STORAGE_KEYS} from '../constants/storage';import {storageService} from '../services/storageService';const C=createContext<{customer:Customer|null;setCustomer:(c:Customer|null)=>void}|null>(null);export function AuthProvider({children}:{children:ReactNode}){const [customer,setCustomer]=useState<Customer|null>(()=>storageService.get(STORAGE_KEYS.CUSTOMER,null));return <C.Provider value={{customer,setCustomer}}>{children}</C.Provider>}export const useAuth=()=>{const c=useContext(C);if(!c)throw new Error('useAuth fora do provider');return c};